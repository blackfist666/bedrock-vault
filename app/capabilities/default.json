{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs (events, invoke) only",
  "windows": ["main"],
  "permissions": ["core:default"]
}
