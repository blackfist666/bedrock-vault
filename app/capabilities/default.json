{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs plus the file picker used by Import",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
